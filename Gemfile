source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in docker_volume_local.gemspec
gemspec

gem "rake", "~> 12.0"
gem 'docker-api', github: 'ComputeStacks/docker-api', ref: '21bf283'
